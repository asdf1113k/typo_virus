[project]
name = "typo-virus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "progressbar>=2.5",
]

[tool.uv.workspace]
members = [
    "venv",
]
