[project]
name = "typo-virus"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alive-progress>=3.3.0",
    "colorama>=0.4.6",
    "progressbar>=2.5",
]

[tool.uv.workspace]
members = [
    "venv",
]
